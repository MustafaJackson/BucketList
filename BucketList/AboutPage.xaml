<Page
    x:Class="BucketList.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BucketList"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Home" Label="Home" x:Name="HomeButton" Click="HomeButton_Click"/>
        </CommandBar>
    </Page.BottomAppBar>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="About" TextAlignment="Center" Style="{ThemeResource HeaderTextBlockStyle}" />

        <TextBlock Grid.Row="1" Text="Bucket List is your friend who remembers what you want and fantasize about, tell him your dreams and he'll save it for you till you get them done." Style="{ThemeResource BodyTextBlockStyle}" Margin="20,50,20,20" TextWrapping="Wrap" />

        <TextBlock Grid.Row="2" Text="for contact : Mustafa.Alaaelden@outlook.com" Style="{ThemeResource BaseTextBlockStyle}" Margin="20" VerticalAlignment="Bottom" />
    </Grid>
</Page>
